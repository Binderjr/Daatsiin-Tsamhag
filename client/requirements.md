## Packages
framer-motion | Smooth page transitions and reveal animations for a premium feel
lucide-react | Modern, clean iconography for the UI
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to resolve Tailwind CSS class conflicts
react-hook-form | Performant, flexible forms for property posting and interactions
@hookform/resolvers | Validation resolvers for React Hook Form (using Zod)
zod | Schema validation
wouter | Minimalist routing
@tanstack/react-query | Data fetching and state management

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
